name: CD

# Deploys muischeduler.2plot.dev, then checks the live site.
#
# The deploy step POSTs to a Render deploy hook held in the
# RENDER_DEPLOY_HOOK_URL secret. Without that secret the step is skipped and
# the workflow goes straight to verification — Render is auto-deploying from
# GitHub on its own, so absence of the secret is a working configuration, not
# a failure.
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target_url:
        description: Site to verify (skips the deploy when set to another host)
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: cd-production
  cancel-in-progress: false

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  SITE_URL: ${{ inputs.target_url || 'https://muischeduler.2plot.dev' }}

jobs:
  test:
    name: ci
    uses: ./.github/workflows/ci.yml

  deploy:
    name: deploy to render
    needs: [test]
    runs-on: ubuntu-latest
    # Long enough for the build-match wait below (up to 100 × 15s) and no
    # longer — without it the job inherits GitHub's six-hour default,
    # which is how a platform that never comes back healthy holds the
    # `cd-production` concurrency group all day. Sized for the WORST build,
    # not the median: a dependency FLOOR BUMP busts the Docker cache by
    # design, so this pipeline's most important deploy is also Render's
    # slowest — a sibling host's wait timed out on exactly that build class
    # (2026-08-23) with the old 60 × 15s window.
    timeout-minutes: 30
    environment:
      name: production
      url: https://muischeduler.2plot.dev
    outputs:
      deployed: ${{ steps.hook.outputs.deployed }}
    steps:
      - name: Trigger the Render deploy hook
        id: hook
        env:
          HOOK: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "$HOOK" ]; then
            # ::warning, not ::notice (2026-08-23): with no hook AND a push
            # Render's autoDeploy happened to skip, nothing deployed at all
            # and the build-match wait below timed out on a deploy nobody
            # had started. The wait refusing was honest; the quiet notice is
            # why the cause took a full run to see.
            echo "::warning::RENDER_DEPLOY_HOOK_URL is not set — this push deploys only if render.yaml autoDeploy fires. The build-match wait below still holds for THIS commit either way."
            echo "deployed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          curl -fsS -X POST "$HOOK" > /dev/null
          echo "deployed=true" >> "$GITHUB_OUTPUT"

      - name: Wait for THIS build to serve traffic
        # NOT gated on the hook step, deliberately. RENDER_DEPLOY_HOOK_URL is
        # unset fleet-wide (a recorded decision: Render's autoDeploy owns
        # deploy timing, CD waits and verifies), so the old
        # `if: steps.hook.outputs.deployed == 'true'` SKIPPED this wait
        # entirely and ran the battery seconds after the push — against the
        # PREVIOUS release, on every run. That defect is invisible for as
        # long as it lasts, because the old build always already passes the
        # old battery; it only surfaces when a run adds a surface the old
        # build does not have. Found on muicharts 2026-08-21, fleet-wide
        # class, fixed here in the 2026-08-22 gate wave.
        run: |
          # A bare 200 proves nothing about WHICH build answered — and this
          # service has a disk, so it RESTARTS with a brief blip rather than
          # overlapping old and new instances. /healthz now reports the
          # running instance's commit (RENDER_GIT_COMMIT, see lib/health.py)
          # and this loop holds until it equals the SHA that triggered this
          # run. Fallback: a build predating the field gets the old
          # sustained-health wait, with a warning naming what it cannot tell
          # — that fallback fires exactly once, on the deploy that carries
          # the field for the first time.
          want="${GITHUB_SHA}"
          matched=0
          ok=0
          for _ in $(seq 1 100); do
            body="$(curl -fsS "$SITE_URL/healthz" 2>/dev/null || true)"
            if [ -n "$body" ]; then
              build="$(printf '%s' "$body" | python3 -c 'import sys,json;print(json.load(sys.stdin).get("build",""))' 2>/dev/null || true)"
              if [ "$build" = "$want" ]; then
                matched=$((matched + 1))
                [ "$matched" -ge 3 ] && break
              elif [ -z "$build" ]; then
                ok=$((ok + 1))
              fi
            fi
            sleep 15
          done
          if [ "$matched" -ge 3 ]; then
            echo "::notice::live /healthz reports build $want — verifying the artifact this run shipped."
          elif [ "$ok" -ge 5 ]; then
            echo "::warning::live /healthz predates the build field — cannot prove WHICH build is serving; verified sustained health only. This warning should appear exactly once."
          else
            echo "::error::$SITE_URL never served this run's build ($want) and never became reliably healthy"
            exit 1
          fi

  verify:
    name: verify the live site
    needs: [deploy]
    if: always() && needs.deploy.result != 'cancelled'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # The network battery first: the same script, with the same check
      # names, that CI ran against the container this deploy shipped. A name
      # that passed in CI and fails here isolates the fault to the deploy.
      - name: Network smoke battery
        run: python scripts/network_smoke.py --base-url "$SITE_URL"

      # Then the satellite-specific checks the battery does not make: every
      # canonical, every crawler body, the CDN card's real pixels, and every
      # peer llms.txt in the directory actually resolving (peers WARN, this
      # host FAILS).
      - name: Smoke-test the deployment
        run: python scripts/smoke_live.py "$SITE_URL"

      - name: Report
        if: failure()
        run: |
          echo "::error::Live verification failed for $SITE_URL. Every failure these check for is silent in production: a site identity fallen back to a framework default, a stale dash-improve-my-llms artifact, a canonical on the wrong host, a page serving the JavaScript stub, a 404ing or reshaped social card, a missing network directory, and dead peer llms.txt links."
